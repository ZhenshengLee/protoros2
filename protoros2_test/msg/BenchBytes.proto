syntax = "proto3";

// must follow: <ros2_package_name>.<folder_name>.pb
package protoros2_test.msg.pb;
// not necessary: enabled by default
option cc_enable_arenas = true;

// Single large bytes field, used to benchmark/verify aliasing (zero-copy) parsing.
message BenchBytes {
  bytes data = 1;
}
