// Generated by https://github.com/foxglove/foxglove-sdk

syntax = "proto3";

import "google/protobuf/timestamp.proto";

// must follow: <ros2_package_name>.<folder_name>.pb
package protoros2_example.msg.pb;

// A compressed image
message CompressedImage {
  // Timestamp of image
  google.protobuf.Timestamp timestamp = 1;

  // Frame of reference for the image. The origin of the frame is the optical center of the camera. +x points to the right in the image, +y points down, and +z points into the plane of the image.
  string frame_id = 4;

  // todo: compile protobuf with absl string to use string_view type or wait for pb >= 3.23 apt pkg
  // Compressed image data
  bytes data = 2;

  // Image format
  //
  // Supported values: `jpeg`, `png`, `webp`, `avif`
  string format = 3;
}
